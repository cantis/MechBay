Generated by: internal code review scan on branch `feature/code-improvement` (April 26, 2026)

---

## Outstanding Items — Actionable List

The following items remain outstanding across the reviews. Each entry includes current status and recommended next steps.

1) Standardize JSON Envelope & Dual-Mode Response Helper
- Status: Partially implemented — most endpoints use the `{ "success": bool, "error": str|null, "data": dict|null }` pattern, but some utility endpoints (`/miniatures/next-id`) and a few success responses return convenience shapes outside the envelope.
- Recommended action:
  - Add a helper `make_dual_response(success, data=None, error=None, status=200)` (e.g., `app/utils/response.py`) and refactor JSON-returning routes to use it.
  - Migrate small utility APIs to return `{success:true, error:null, data:{...}}` to keep parsing consistent for clients.

2) DB-level Single-Active-Force Constraint / Test
- Status: Not implemented — the single-active-force invariant is enforced in application logic only; the code documents the SQLite limitation preventing a partial unique index.
- Recommended action:
  - If planning PostgreSQL, add a migration to create a partial unique index: `CREATE UNIQUE INDEX idx_one_active ON forces (is_active) WHERE is_active = true` and add an integration test asserting DB rejects two active forces.
  - Alternatively, add an explicit application-level test that simulates concurrent activations and asserts only one force ends up active (helps detect regressions until DB-level constraint is added).

3) Startup Fail-Fast for Missing `SECRET_KEY` in Production
- Status: Not implemented — current behavior generates a random key and logs a warning (safe for development but not ideal for production).
- Recommended action:
  - Add a startup validation in `create_app()` that raises an error (or exits) when `SECRET_KEY` is unset and the app is not in `TESTING` and not in a clearly development mode.
  - Document required env vars in README/Deploy docs and ensure CI sets them for integration tests where persistence across restarts matters.

4) JS fetch() Error Handling Sweep — Replace Empty `.catch()` Handlers
- Status: Partially implemented — most `fetch()` calls have `.catch()` with user alerts; one remaining weak spot: `app/static/js/miniatures.js` uses `.catch(() => {})` for the series -> next-id call, which swallows failures.
- Recommended action:
  - Update `app/static/js/miniatures.js` series change handler to use a visible failure path, e.g. `.catch(() => { alert('Request failed. Please try again.'); console.warn('next-id fetch failed'); });`.
  - Optionally run a quick grep for `.catch(() => {})` and replace with a standard handler.

---

If you want, I can implement (1) the `make_dual_response` helper and refactor routes, or (4) fix the JS `.catch()` occurrences now — which should I do first?
---
description: "Detailed MechBay code review findings — April 26, 2026 (feature/code-improvement)"
mode: "note"
---

# MechBay Code Review — Detailed Findings

This document records the findings from a focused scan of the `feature/code-improvement` branch on April 26, 2026. It expands the short summary and maps each recommendation to concrete locations in the repository, the current status, and recommended next steps.

## Executive Summary
- Overall: Good structure (models → services → blueprints), solid test coverage at service + many route levels.
- Major items: Security defaults, error handling & logging, input validation, tests, and JSON/dual-mode consistency.

---

## 1. Fix Security Defaults Before Any Public Deployment

Status: Implemented (with acceptable behavior for development/testing)

What I checked:
- `app/config.py` — Generates a random `SECRET_KEY` when `SECRET_KEY` env var is unset and logs a warning. Also sets `MAX_CONTENT_LENGTH`.
  - File: app/config.py
- `server.py` — Uses `app.config.get("DEBUG", False)` to control Flask debug mode instead of hardcoding `True`.
  - File: server.py
- `app/static/js/forces.js` — No use of `innerHTML` for user-provided values; text values inserted via `textContent` and DOM methods.
  - File: app/static/js/forces.js

Notes and recommendations:
- The current approach (generate random key + log warning) is safe for development. For production, require `SECRET_KEY` or fail fast (consider adding startup validation to exit when not provided and not in testing).
- `WTF_CSRF_SECRET_KEY` defaults to `SECRET_KEY` — acceptable but call out in deploy docs to set explicit envs.

---

## 2. Add Custom Error Handlers and Structured Logging

Status: Implemented

What I checked:
- Custom error handlers for 400, 404, 500 are present and return the standard JSON envelope for `request.is_json` and a branded template for browser requests.
  - File: app/__init__.py (errorhandler functions)
- Structured logging setup using `structlog` configured in `app/logging.py`; `create_app()` calls `setup_logging()` at startup.
  - File: app/logging.py
- Tests exist verifying 404 JSON and HTML behavior.
  - File: tests/test_error_handlers.py

Notes and recommendations:
- Logging is configured and used (many modules call `structlog.get_logger()` and log events). Continue to use named log events and structured fields for observability.


## 3. Harden Input Validation at Route Boundaries


What I checked:
- Route-level `int()` conversions are wrapped in try/except in `forces.py` and `miniatures.py` (examples: `add_miniature`, `remove_miniature`, `move_miniature`, pagination parsing in `miniatures.list_miniatures`).
  - File: app/blueprints/forces.py
- `update_miniature()` uses an explicit `ALLOWED_UPDATE_FIELDS` whitelist; import uses `ALLOWED_IMPORT_FIELDS`.
- `MAX_CONTENT_LENGTH` is set in `app/config.py` (10 MB).
  - File: app/config.py

Notes and recommendations:


Status: Largely implemented; one DB-level invariant test missing

What I checked:
- There are comprehensive route tests for forces and lance templates covering success and many negative cases (missing params, invalid IDs, duplicate adds, import failures).
  - Files: tests/test_forces.py, tests/test_lance_templates.py
- Negative and edge-case tests present for bulk actions, imports, and missing resources.
  - Files: tests/test_error_handlers.py, tests/test_forces.py

Missing / Limitation:
- The prompt recommended a test that validates a database-level partial unique index preventing two active forces. The codebase documents that this is a known limitation for SQLite and the service enforces a single active force in application logic but does not create a DB-level partial unique index. There is no test asserting a DB constraint for `is_active` uniqueness.
  - File: app/services/force_service.py (notes about known limitation)

Recommendation:
- Add a unit/integration test asserting the application-level behavior that only one force is `is_active=True` after concurrent-ish operations, or if switching to PostgreSQL in CI, add a migration and a test for a partial unique index.

---

## 5. Standardize Error Response Format and Dual-Mode Pattern

Status: Partially implemented; mostly consistent but a few endpoints differ

What I checked:
- Most JSON responses include `success` and `error` keys. Error handlers use the `{"success": False, "error": "..."}` envelope.
  - Examples: app/blueprints/forces.py, app/blueprints/miniatures.py
- Some endpoints return different shapes for convenience (e.g., the `next_id` endpoint returns `{"next_id": n}`) and some success responses return additional fields directly (e.g., `{